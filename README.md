# BabySense

육아 기록/모니터링 Flutter 앱 + Claude를 부르는 FastAPI 서버.
로그인·회원가입, 아이 정보 온보딩, 홈 대시보드, 수유/체온/배변/수면/성장 기록,
소음 측정, 예방접종, 약·병원, 커뮤니티, 함께 키우기(공동 육아)로 구성됩니다.

**판정이 있는 영역은 체온·성장·소음 셋뿐입니다.** 나머지는 기록하고 집계만
합니다 — 임계값을 정한 공인 출처를 찾지 못했고, 없는 기준을 지어내는 것보다
그 편이 낫다고 판단했습니다([`docs/assessment-rules.md`](docs/assessment-rules.md)).

**이 앱은 진단하지 않습니다.** 피부 사진도 병명을 말하지 않고, 보이는 것과
단계만 돌려줍니다.

하단 탭 5개: **홈 · 기록 · 분석 · 커뮤니티 · 전체**

## 지금까지 만든 것

2026-04-10에 시작해 커밋 139개입니다. 앱 `lib/` 102개 파일 19,540줄, 테스트
67개 파일 8,800줄, 서버 12개 파일, 마이그레이션 11개.

### 앱만으로 도는 것

로그인·회원가입(이메일 + 구글·카카오), 아이 정보 온보딩과 수정, 함께 키우기,
기록 7종(수유·배변·수면·체온·성장·약·병원), 24시간 원과 월·주·일 눈금으로
보는 기록 탭, 주간 요약, 예방접종과 접종 전 확인, 커뮤니티, 다크 모드(시간에
따라 자동), 수유·예방접종 알림. **전부 Supabase에 실제로 저장되고 조회됩니다.**

판정은 **체온·성장·소음 셋뿐**입니다. 나머지는 기록하고 세기만 합니다 —
임계값을 정한 공인 출처를 찾지 못했고, 없는 기준을 지어내는 것보다 그 편이
낫다고 판단했습니다.

### 서버가 떠 있어야 도는 것

피부 사진 판독과 영역별 상담 둘입니다. 둘 다 FastAPI를 거쳐 Claude를 부릅니다.
**서버가 없으면 이 둘만 "서버에 연결하지 못했습니다"가 됩니다** — 나머지
기능은 그대로 동작합니다.

### 아직 안 되는 것

푸시 알림(FCM — 설정 파일도 코드도 없습니다), 애플 로그인(단추는 있지만
안내 문구만 띄웁니다), '전체' 탭의 육아 가이드·진료용 리포트·공지사항·
1:1 문의(준비 중), 서버 배포(배포 파일이 없습니다).

플랫폼별로는 **소음 측정이 macOS에서 안 됩니다**(플러그인 미지원). iOS는
`BGAppRefreshTask` 때문에 연속 측정이 불가능하고, 안드로이드만 foreground
service로 계속 잽니다.

### 중간에 되돌린 결정 둘

- **피부를 자체 CNN에서 Claude 비전으로 바꿨습니다.** 붙어 있던 체크포인트는
  클래스가 성인 병변 이름이었고, 보호자가 실제로 찍는 자리(기저귀 발진·
  땀띠·태열·지루성 피부염)는 라벨조차 없었습니다. 학습 코드가 없어 재현도
  불가능했습니다. 지금은 병명을 말하지 않고 **보이는 것과 단계만** 돌려줍니다.
- **소음 원시 로그를 버리고 집계만 남겼습니다**(011). 밤새 초 단위로 쌓이는
  행이 실익 없이 무거웠습니다.

## 시작하기

> ⚠️ **저장소를 처음 받았거나 pull 후 앱이 안 켜진다면 이 절차를 먼저 확인하세요.**
> Supabase 연결 정보를 소스에 넣지 않고 `env.json`에서 주입받는데, 이 파일은
> `.gitignore`에 있어 저장소에 포함되지 않습니다. 없으면 앱이 시작하자마자 멈춥니다.

**1. `env.json` 만들기**

`env.example.json`을 복사해 `env.json`으로 이름을 바꾸고 값을 채웁니다.
값은 팀 내부에서 따로 공유받으세요 (**저장소에 커밋하지 마세요**).

```json
{
  "SUPABASE_URL": "https://<프로젝트ID>.supabase.co",
  "SUPABASE_PUBLISHABLE_KEY": "<publishable key>"
}
```

**2. 실행할 때 `env.json`을 넘기기**

이 인자를 빠뜨리면 앱이 시작하자마자 멈춥니다.

| 환경 | 방법 |
|---|---|
| 터미널 | `flutter run --dart-define-from-file=env.json` |
| **VS Code** | 실행 구성에서 **BabySense (env.json)** 선택 (`.vscode/launch.json`에 포함) |
| **Android Studio** | 실행 구성에서 **main.dart (env.json)** 선택 (`.run/`에 포함) |

목록에 안 보이면 IDE를 다시 열거나 아래처럼 직접 설정하세요.

- VS Code: `.vscode/launch.json`의 `toolArgs`에 `--dart-define-from-file=env.json`
- Android Studio: Run/Debug Configurations → **Additional run args**에 같은 값

**3. Supabase 스키마**

- **DB를 새로 만드는 사람**: [`supabase/schema.sql`](supabase/schema.sql) 전체를
  Supabase 대시보드의 SQL Editor에 붙여넣고 실행합니다.
- **이미 쓰던 DB가 있는 사람**: [`supabase/migrations/`](supabase/migrations/)의
  파일을 번호 순으로 실행합니다. 여러 번 실행해도 안전합니다.

> ✅ **004(함께 키우기)는 2026-08-11 운영 DB에 적용됐습니다.**
> 새 DB를 만드는 경우에도 `schema.sql`에 이미 포함되어 있습니다.

테이블 설계는 [`docs/erd.md`](docs/erd.md) 참고.

소셜 로그인 설정은 [`docs/social-login-setup.md`](docs/social-login-setup.md)에 있습니다.

## 백엔드 구성

```
Flutter
 ├─> Supabase        Auth(인증) · PostgreSQL(모든 기록) · Storage(피부 사진)
 ├─> FastAPI (:8000) 육아 질문 답변 · 피부 AI 추론  ← 저장소의 server/
 │     └─> Claude API
 └─> Firebase        FCM 푸시 알림 (예정)
```

MySQL과 Spring 서버는 사용하지 않습니다. **FastAPI 서버는 DB에 붙지 않습니다.**
인증·기록 CRUD·Storage는 앱이 Supabase와 직접 처리하고, 분석 결과를 저장하는 것도
앱이 하므로 서버가 사용자 JWT를 중계할 필요가 없고 RLS가 그대로 적용됩니다.
서버 실행 방법은 [`server/README.md`](server/README.md)를 참고하세요.

**규칙 기반 판정은 앱에서 계산합니다.** 네트워크 없이도 즉시 안내를 띄우기 위해서이고,
판단 근거의 추적 가능성은 판정 시점의 입력값 스냅샷과 규칙 버전(`rule_version`)을 함께
저장해 확보합니다. 서버는 **외부 AI 호출과 피부 추론에만** 씁니다 — API 키가 앱에
포함되면 역컴파일로 탈취될 수 있기 때문입니다.

| 엔드포인트 | 상태 |
|---|---|
| `POST /api/advice` | ✅ `ANTHROPIC_API_KEY`가 있으면 동작 |
| `POST /api/skin/diagnose` | ✅ Claude 비전. **진단하지 않습니다** |

인증·기록·판정은 **서버 없이도 전부 동작합니다.**

### 서버가 필요한 경우

**육아 질문 답변**이나 피부 분석을 쓸 때만 필요합니다. 터미널 두 개를 씁니다.

```bash
# 1) AI 서버
cd server && source venv/bin/activate
set -a; source .env; set +a          # ANTHROPIC_API_KEY를 읽습니다
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# 2) 앱
flutter run --dart-define-from-file=env.json
```

> 🔑 **`server/.env`에 `ANTHROPIC_API_KEY`가 필요합니다.**
> 이 파일은 `.gitignore`로 막혀 있습니다. **앱(`env.json`)에는 넣지 마세요** —
> Flutter 패키지는 뜯어서 키를 꺼낼 수 있습니다.
>
> ```
> ANTHROPIC_API_KEY=sk-ant-...
> ```
>
> 키가 없어도 서버는 정상 기동하고 `/api/advice`·`/api/skin/diagnose`만
> 503을 반환합니다. **두 엔드포인트가 같은 키 = 같은 크레딧**을 쓰므로
> 횟수 상한을 기능별로 따로 셉니다(상담 30회/시간, 피부 10장/시간).
> `/health`의 `advice.ready`로 확인할 수 있습니다.

실기기에서는 기본 주소(`10.0.2.2` / `127.0.0.1`)가 닿지 않으므로 개발 PC의 LAN IP를
넘겨야 합니다: `--dart-define=API_BASE_URL=http://192.168.0.10:8000`

## 프로젝트 구조

```
lib/
  core/
    constants/   # AppColors, AppRadius, AppSpacing, AppTextStyles, ApiConfig,
                 # SupabaseConfig, WhoGrowthStandards
    services/    # BabyService, NoiseTracker, SleepType, GrowthCalculator,
                 # NotificationSetup(알림 초기화 한 곳), NotificationTest
    theme/       # AppTheme
    widgets/     # CommonButton, CommonTextField, CommonAppBar
  features/      # 화면 단위 폴더
    auth/           # 로그인 · 회원가입 · auth_gate · post_auth_route(로그인 후 분기)
    onboarding/     # 아이 정보 최초 등록 (등록 폼은 이 화면 한 곳에만 존재)
    shell/          # MainShell(하단 탭 5개) · 전체 탭 · 스플래시 · 준비 중 안내
    home/           # 홈 탭 · TodaySummary(오늘의 마지막 기록)
    records/        # 기록 탭 — 하루를 24시간 원으로 · 월/주/일 눈금 · 합친 목록
    analysis/       # 분석 탭 — 최근 7일 집계 · assessments 조회
    advice/         # 육아 질문 — 서버를 거쳐 Claude에 물어봅니다
    detail/         # 기록 화면 + 화면별 *_service.dart (Supabase 접근)
    detail/growth/  # 성장 기록 모델 · GrowthRepository · 페이지
    detail/assessment/ # 판정 규칙(체온·성장·소음)과 assessments 저장·조회
    feeding_reminder/     # 다음 수유 시각·수유 알림 예약
    vaccination_reminder/ # 예방접종 예정일 알림 예약
    community/      # 게시글 · 댓글 (작성 · 수정 · 삭제)
    mypage/         # 내 정보 · 아이 · 함께 키우기(초대 코드)
    settings/       # 알림 · 화면 · 앱 정보 · 로그아웃
  routes/        # AppRoutes (라우트 문자열 상수)
  main.dart      # 앱 진입점 + Supabase 초기화 + 백그라운드 소음 측정 서비스
server/          # FastAPI 추론 서버 (피부)
test/            # lib/ 구조를 1:1로 반영 (test/core/services, test/features/...)
docs/            # ERD, 소셜 로그인 설정, Supabase 남은 작업, 논문용 문서
supabase/        # schema.sql (테이블 · RLS · 마스터 데이터), migrations/
```

- 공통 스타일은 `core/constants`의 상수와 `core/widgets`의 공통 위젯으로 관리
- 네비게이션은 `AppRoutes` 상수 경유가 원칙 (일부 화면은 `MaterialPageRoute` 직접 사용도 혼재)
- 서버 주소는 `ApiConfig` 한 곳에서만 정의합니다. 화면에 URL을 하드코딩하지 마세요
- Supabase 접근은 화면에서 직접 호출하지 않고 `core/services` 또는 feature별 서비스를 경유합니다
- **테스트 자료에 실제 정보를 쓰지 않습니다.** 공개 저장소이고 아이 이름은
  개인정보입니다. 자리만 채우면 되는 값에는 `지호`·`아기`처럼 누가 봐도
  가짜인 것을 씁니다
- 테스트는 대상 파일과 같은 경로·같은 이름에 둡니다
  (`lib/features/mypage/baby_member_service.dart` → `test/features/mypage/baby_member_service_test.dart`).
  여러 파일에 걸친 동작을 검사하는 테스트만 예외입니다

> **`features/detail/`은 이름이 낡았습니다.** `DetailPage`에서 따온 이름인데 그 화면을
> 지웠습니다. 지금은 기록·소음·AI·판정이 한 폴더에 섞여 있습니다. 이 폴더를 가리키는
> import가 74곳이라 팀원 작업과 충돌할 위험이 커서 미뤄 뒀습니다.

## 코딩 가이드라인

이 저장소는 [`CLAUDE.md`](CLAUDE.md)의 원칙(가정하지 않고 확인하기, 최소한의 변경, 외과적 수정, 검증 가능한 목표)을 따릅니다.

## 최근 변경 사항

2026-08-18~19에 기록 탭을 다시 만들었고, 그 앞 2026-08-12~13에 크게 세 덩어리가
바뀌었습니다. 각각 왜 그렇게 됐는지가 코드 주석에 남아 있습니다.

### 전체 점검에서 나온 것들

실제 계정을 만들어 한 주치를 넣고 앱을 돌린 뒤, 네 관점(새 기능 완결성 ·
빈 상태/첫 사용 · 실패와 경계 · 읽는 사람)으로 훑었습니다. 지적 24건 중
반증을 견딘 16건을 고쳤습니다(서머타임 관련 셋은 한국 전용이라 뺐습니다).

**끝나지 않은 수면 한 건이 여러 날을 24시간 수면으로 채웠습니다.** 소음
측정을 켜 두고 끝맺지 못한 행이 실제로 생기는데, 그것을 '지금까지 자는 중'
으로 보고 그렸습니다. 9월 1일 21시에 열린 행 하나가 **18일을 꽉 채웠습니다.**
하루가 넘도록 열려 있으면 재는 중인 잠이 아니라 끝맺지 못한 기록으로 봅니다 —
`weekly_summary.dart`가 이미 하던 판단("끝난 수면만 더합니다")과 같습니다.

**같은 잠을 화면마다 다르게 불렀습니다.** 원 카드는 '50분', 목록과 홈 타일은
'0시간 50분'. 세 자리가 각자 적고 있었습니다. `formatDuration`을
[`core/services/duration_text.dart`](lib/core/services/duration_text.dart)로
옮겨 모두 같은 함수를 지나가게 했습니다. 이제 시간을 손으로 적는 자리가
저장소에 없습니다.

**다크 모드에서 원의 본문이 안 보였습니다.** 수면 호를 `Colors.indigo`로 박아
두어 어두운 고리 대비가 1.50:1이었습니다. 색을 생성자로 뺀 장치가 있었는데
넘기는 값이 리터럴이라 놀고 있었습니다. 팔레트에 테마별 값을 두었습니다.
재 보니 **배변 점은 밝은 테마가 더 나빴습니다**(흰 카드 위 2.04:1).

**'아이 정보를 먼저 등록해 주세요'가 막다른 길이었습니다.** 기록·분석 두
탭이 안내만 하고 갈 길을 주지 않아, 전체 탭 → 마이페이지를 스스로 찾아내야
했습니다. 피부 분석은 한술 더 떠 '불러오지 못했습니다. 잠시 후 다시 시도해
주세요'라고 **원인을 틀리게** 짚었습니다 — 다시 찍어도 영원히 같습니다.

그 밖에 커뮤니티 탭만 실패 시 보던 글을 지우던 것, 분석 탭의 한 분기에만
경쟁 방어가 빠져 있던 것, 작은 원이 정작 자기가 그리는 수면을 낭독하지 않던
것을 고쳤습니다.

### 홈과 기록 탭 — 화면이 스스로와 어긋나던 둘

검증용 계정을 만들어 한 주치를 넣고 앱을 실제로 돌려 보다 나온 것들입니다.

**홈 타일이 부제목을 받아 놓고 그리지 않았습니다.** `_tileSubtitle`이
'불러오는 중…'·'불러오지 못함'·'기록 없음'을 갈라 만들고 아홉 타일이 모두
그 값을 넘기는데, 타일은 아이콘과 제목만 그렸습니다. 이력을 훑어보니
**한 번도 그린 적이 없습니다.**

그래서 홈에서는 조회 실패와 기록 없음이 똑같아 보였습니다. 이 저장소가 맨
앞에 적어 둔 원칙이 정작 홈에서 지켜지지 않고 있었습니다. 마지막 수유가
언제였는지도 홈에 나온 적이 없습니다.

`test/silent_failure_test.dart`의 '홈 타일이 세 상태를 구별한다'가 이것을
못 잡았습니다. **소스에 그 문자열이 있는지만 봤고, 문자열은 있었습니다.**
화면에 닿는지를 보는 테스트로 옮겼습니다.

**기록 탭에서 원과 목록이 다른 말을 했습니다.** 자정을 넘는 밤잠은 두 원에
나뉘어 그려지지만 목록에는 시작한 날에만 들어갑니다(`RecentRecord.at`이
시작 시각입니다). 그래서 원은 여섯 시간을 잤다고 그리는데 바로 아래에서
'이 날은 남긴 기록이 없습니다'라고 말하는 날이 생겼습니다.

    9/2 원   : 호 1개, 수면 375분
    9/2 목록 : 0건 → "이 날은 남긴 기록이 없습니다."

원에 잠이 그려져 있으면 왜 목록이 비는지 밝힙니다. 원도 비었으면 그냥
없다고 합니다 — 없는 사연을 지어내지 않습니다.

### 피부 — 분류기를 붙였다가 되돌린 일

피부 분석에 MobileNetV2 분류기(`server/baby_skin_mobilenet_model.pth`)를 1차
필터로 붙인 적이 있습니다. 되돌렸습니다. 아래는 그때 확인한 것들입니다.

**인증이 통째로 열려 있었습니다.** 새 라우터를 `include_router`로 등록했는데
그것이 **같은 경로의 기존 엔드포인트보다 먼저** 등록됐습니다. FastAPI는 등록
순서로 매칭하므로, `Depends(auth.require_user)`와 개월 수 필수와 호출 상한을
달고 있던 원래 엔드포인트가 통째로 가려졌습니다. 로그인 없이 사진을 넣고
Claude 크레딧을 태울 수 있는 상태였고, 문이 두 개(`/api/skin`, `/skin`)였습니다.
로컬에서만 닿는 상태라 밖으로 새지는 않았습니다.

**그 구멍을 지키던 테스트도 같이 꺼져 있었습니다.** `tests/test_skin.py`가
수집 단계에서 실패해 **36개가 통째로 안 돌았습니다.** 그 안에
`test_rejects_anonymous`와 `test_does_not_call_claude_when_anonymous`가
있었습니다. 구멍을 뚫으면서 경보기를 같이 끈 셈입니다. Flutter 쪽도 16개가
깨졌는데, 하필 '정상이라고 말하지 않는다'·'결과 카드가 진단이 아님을 적는다'
같은 안전 계약들이었습니다.

**모델을 실제로 돌려 봤습니다.**

    ISIC 더모스코피 '점' 사진 120장 → 94.2% 정답, 신뢰도 99.0%
    무작위 노이즈                  → "곰팡이·백선" 88.5% 확신
    사물·풍경 사진 60장            → 41.7%가 "normal"

못 만든 모델이 아닙니다. **자기가 배운 것(성인 병변 더모스코피)은 잘 합니다.**
문제는 이 앱이 보내는 것이 휴대폰으로 찍은 영유아 피부 사진이라는 데 있습니다.

**`normal`이 피부를 보고 있지 않았습니다.** 더모스코피 사진에서 'normal'
확률은 0.0%인데 일반 카메라 사진에서는 38.9%였습니다 — **5,013배.** 질환
이미지와 정상 이미지를 **다른 데이터셋에서** 가져오면 모델은 병이 아니라
'어느 데이터셋에서 왔는가'를 배웁니다. 휴대폰 사진은 후자 쪽이라, 발진이
있어도 '정상'으로 기울 수 있었습니다.

**신뢰도 임계값으로는 못 막습니다.** 사물 사진 60장 중 99% 이상으로 답한 것이
12%였고 최댓값은 100%였습니다. 정상 입력에서 맞혔을 때 평균이 99.8%라
임계값을 더 올리면 맞는 답까지 버립니다. 한 축으로는 분리되지 않습니다.

그래서 서버·화면·라우터를 되돌리고 판단은 Claude 쪽에 두었습니다. **모델
파일은 지웠습니다가 아니라 남겨 두었습니다** — 학습 데이터 출처와 성능 근거가
정리되면 인증 뒤에서 다시 붙일 수 있습니다. 지금 코드는 `torch`를 쓰지
않으므로 서버를 띄우는 데 추가 설치가 필요 없습니다.

### 피부 — 보호자가 실제로 찍는 자리

프롬프트에 기저귀가 닿는 곳·접히는 곳·얼굴과 머리를 넣었습니다. 폐기한 성인
데이터셋에는 기저귀 발진·땀띠·태열이 **라벨조차 없었는데**, 정작 이 앱에 사진이
올라오는 이유가 그 자리들입니다.

**병명은 한 글자도 넣지 않았습니다.** 각 자리마다 '볼 것'과 '할 수 있는 것'만
적습니다. "주름 안쪽까지 들어갔는지"는 보이는 것이고 "칸디다"는 진단입니다 —
앞의 것만 씁니다. 힌트가 뒷문으로 진단이 되지 않도록 절 안에 못을 박았습니다:
*"이것으로 병을 가리려 하지 마세요. 보이는 것만 적고, 그것이 무엇을 뜻하는지는
쓰지 마세요."* 그리고 얼굴 사진에 "기저귀를 자주 갈아 주세요"가 붙지 않도록
*"사진에서 그 자리로 보일 때만 씁니다"*도 함께 두었습니다.

### 알림 — macOS에서 통째로 죽어 있던 것 · 서머타임 · 문구

예방접종 알림과 '알림 시험'은 팀원이 넣었습니다. 아래는 그것을 검증하면서
드러난 것들입니다.

**macOS에서 알림이 하나도 뜨지 않았습니다.** `flutter_local_notifications`는
`InitializationSettings`에 `macOS` 항목이 없으면 **초기화 단계에서 예외를
던집니다**(`macOS settings must be set when targeting macOS platform`).
그 설정을 **네 곳**이 각자 적어 두었고, 네 곳 모두에서 빠져 있었습니다 —
수유·예방접종·알림 시험·백그라운드 소음 서비스. 앱을 띄워 로그를 보고서야
알았습니다.

지금은 [`lib/core/services/notification_setup.dart`](lib/core/services/notification_setup.dart)
한 곳에서 만듭니다. 다른 곳에서 직접 만들면 테스트가 막는데, **그 테스트를
쓰자마자 네 번째 사본(`main.dart`)이 걸렸습니다.** 권한을 묻는 것은 사용자가
직접 누르는 알림 시험뿐입니다 — 예약은 앱이 알아서 거는 일이라 그때 권한
창이 뜨면 뜬금없고, 모바일은 앱 시작 때 `permission_handler`가 한 번에
받지만 macOS에는 그 경로가 없습니다.

**예방접종 알림이 서머타임 지역에서 하루 일찍 울렸습니다.** 예정일 자정에서
`Duration(days:)`를 빼면 절대 시간이라 전환일에는 전날 23시가 되고, 거기서
날짜를 꺼내 9시를 붙이면 하루가 통째로 밀립니다. `America/New_York`에서
3월 9일의 하루 전이 **3월 7일**로 나왔습니다. 달력 날짜로 옮기게 고쳤습니다.

한국은 서머타임이 없어 이 결함은 **동작 테스트로 잡히지 않습니다** — 두
방식이 같은 답을 냅니다. 그래서 그 함수가 `Duration`을 쓰지 않는지 글자로
확인하는 단언을 함께 뒀고, 되돌려 놓고 그 단언이 깨지는 것을 확인했습니다.

**알림 문구에 아이 이름을 넣었습니다.** 함께 키우는 두 사람이 각자 기기에서
받으므로 누구 이야기인지가 제목에 있어야 합니다.

    수유할 시간이에요                  →  지호 수유 시간이 되었어요
    오후 2:10로 정하신 시간이 되었어요.  →  마지막 수유 오후 2:10 · 3시간 간격

이름을 모르면 '아이'처럼 **지어내지 않고** 이름 없이 알립니다. 본문은 다음
예정 시각이 아니라 마지막으로 먹인 때와 정한 간격을 적습니다 — 왜 지금
울리는지가 그것이고, 이 앱이 "먹여야 한다"가 아니라 "정한 간격이 지났다"만
말하는 것과도 맞습니다. 조사도 함께 사라졌습니다: `'2:10로'`는 앞 숫자를
읽는 방식에 따라 `'로'`와 `'으로'`로 갈리는데 늘 `'로'`로 박혀 있었습니다.

### 기록 탭 — 24시간 원과 월·주·일 눈금

기록 탭은 남긴 것을 시간순으로 늘어놓은 목록 하나였습니다. 그것만으로는
"요즘 밤에 몇 번 깨는가"가 보이지 않습니다 — 같은 새벽 3시가 줄글로는 스무 줄
떨어져 있고, 원 위에서는 같은 자리에 겹칩니다.

지금은 하루가 **24시간 원** 하나입니다(방학 생활계획표와 같은 모양). 0시가
12시 방향이고 시계방향으로 돕니다. 수면은 원 위의 호, 수유·배변은 둘레의
점입니다. 위의 **월 · 주 · 일** 단추로 눈금을 바꿉니다 — 달이면 달력에 원
28~31개, 주면 월~일 7개, 일이면 그 하루만. 기존 목록은 고른 날짜의 것으로
아래에 그대로 남습니다.

- **자정을 넘는 밤잠은 두 원에 나뉩니다.** 20시 취침·6시 기상은 월요일 원의
  20~24와 화요일 원의 0~6입니다. 한쪽에만 그리면 밤잠이 통째로 사라집니다.
- **원에는 수면·수유·배변만** 그립니다. 체온·성장·약·병원은 하루에 반복되지
  않아 '패턴'이 되지 않고, 넣으면 원이 점으로 뒤덮여 밤잠이 안 보입니다.
  그것들은 아래 목록이 맡습니다.
- **달력을 `GridView`로 짜지 않았습니다.** 이 탭에 기록 입구 격자를 두지
  않는다는 원칙을 테스트가 'GridView 없음'으로 지키고 있어서, 달력을 격자로
  만들면 그 방어가 통째로 풀립니다. `Column`+`Row`입니다.
- 날짜 계산은 전부 `DateTime(y, m, d + n)`입니다. `add(Duration(days:))`는 절대
  시간이라 서머타임이 낀 구간에서 자정이 밀립니다.
- 배변만 아이콘 대신 💩입니다. 물방울 아이콘은 소변으로도 땀으로도 읽혔습니다.
  홈 타일은 기존 아이콘 그대로라 이 탭에서만 다릅니다.

**조회에 상한이 없던 것**이 이 과정에서 드러났습니다. 서비스들은 최신순으로
정렬해 한도만큼 자르는데 하한(`since`)만 줬으므로, 한도가 **오늘 쪽부터**
채워집니다. 하루 열두 번 먹이는 시기에 2주 전을 펴면 그 날 기록이 한 건도
오지 않고, 화면은 그것을 '수유 0회'라고 단정했습니다. 여섯 조회에 `until`을
더했습니다(기존 호출부 20곳은 그대로 — 안 주면 걸리지 않습니다).

화면이 '오늘'을 묻는 자리와 기간을 읽는 자리를 밖에서 받게 했습니다
(`clock`, `loader`). 이 화면에서 결함이 난 곳은 계산이 아니라 **조립부**였고
— 눈금 전환, 기간 이동, 실패 뒤의 화면, 자정 넘김 — 그 자리들이 조회와 시계를
거치므로 Supabase 없이는 한 줄도 돌려 볼 수 없었습니다.

### 피부 — 진단을 그만두고 Claude 비전으로

성인 피부암 데이터셋(ISIC 계열 9종)으로 분류기를 만들려던 계획을 접었습니다.
영유아에게 거의 없는 질환들이라 **기저귀 발진 사진에 '흑색종'이 붙을 수**
있었고, 보호자가 실제로 사진을 찍는 이유(기저귀 발진·땀띠·태열·지루성
피부염)는 그 데이터에 라벨조차 없었습니다.

지금은 서버가 Claude 비전을 부르고, 돌려주는 것은 셋뿐입니다 — **보이는 것**,
**단계**(주의/상담 권장), **지금 할 수 있는 것**. 병명도 확률도 없습니다.

- `level`에 `normal`이 **없습니다.** 사진 한 장으로 '정상'이라고 말하는 것은
  안심이 아니라 반대 방향의 진단입니다. 체온은 공인 임계값이 있어 정상을
  말할 수 있지만 사진에는 그 근거가 없습니다.
- 응답에 `unknown` 필드가 **필수**입니다("사진으로는 가려운지 알 수 없어요").
  한 덩어리 글로 받으면 "모른다"가 가장 먼저 빠지는 문장이라, 자리를 서버가
  정해 뒀습니다.
- 모델이 낸 값을 코드가 한 번 더 잠급니다(`skin._settle`). **내리지 않고
  올리기만** 합니다 — 구조화 출력은 모양만 보장해서
  `{"level":"caution","urgent":true}`도 스키마를 통과합니다.
- 열은 사진에 없습니다. 앱이 최근 12시간 체온 기록에서 찾아 함께 보냅니다.
  생후 3개월 미만 + 발열이면 코드가 `urgent`를 켭니다.

관점 4개와 적대적 검증으로 프롬프트를 점검하다 **제가 쓴 프롬프트가 스스로
모순된 것**을 찾았습니다 — "암은 다루지 않습니다" 스무 줄 아래에 흑색종
ABCDE 기준("모양이 고르지 않은 점")과 비치유성 병변 기준이 남아 있었습니다.

### 소음 — 원시 로그를 버리고 메모리 집계로

`sleep_noise_logs`에 1초마다 한 행씩 쌓았습니다. 하룻밤이면 **28,800행**입니다.
그런데 그 로그를 읽는 곳은 평균·최대를 구하는 함수 하나뿐이었고, 인덱스
주석이 근거로 든 "그래프 조회용" 화면은 만든 적이 없습니다.

원본을 서버까지 나르려고 붙은 장치들이 이 기능의 결함을 만들었습니다 —
행 상한에 잘려 밤 통계가 '측정 직후 몇 분'이 됐던 것, 004가 2단계 RLS를
놓쳐 밤새 재고도 0건이던 것, 망이 끊기면 8시간을 재고도 결과가 없던 것.

**소음 수치는 이제 어디에도 따로 저장하지 않습니다.** 재는 동안 화면에
실시간으로 보여주고, 끝나면 앱이 메모리에서 집계해 판정 한 행에만 남깁니다
(`assessments.inputs`). 판정 경로에서 네트워크가 통째로 빠졌습니다.

'가장 컸던 소리'는 **평활 전 값**에서 셉니다. 예전에는 이동평균(이전 85% +
새 값 15%)을 거친 값에서 세어, 배경 40dB에 90dB가 한 번 들어와도 45.25dB로
기록됐습니다. 아이를 깨운 그 소리를 놓치고 있었습니다.

끝맺는 흐름은 여러 번 고쳤습니다 — 재진입 방어, 8초 저장 상한, 세션 일련번호,
`stopService`가 세션을 끝맺지 않던 것, 밤새 재고 아침에 중지하면 판정이
안 나오던 것.

### 백그라운드 로그인을 화면에서 분리

백그라운드 isolate가 `Supabase.initialize`를 한 번 더 부르면서 **화면 쪽과
같은 로그인 저장소**를 썼습니다. Supabase의 갱신 토큰은 한 번 쓰면 버려지는
티켓이라, 두 쪽이 같은 서랍에서 꺼내 가면 사슬이 끊깁니다.

    21시  재우기 시작. 화면이 꺼지면 화면 쪽은 갱신을 멈춤
          백그라운드만 밤새 티켓을 갈아치움
    07시  앱을 열면 화면이 몇 시간 전 티켓을 내밂 → 거절 → 로그아웃

그다음이 더 나빴습니다. 로그아웃된 채로 조회하면 오류가 아니라 **빈 목록**이
옵니다(RLS가 '이 사람 것'만 주는데 그 사람이 없으므로). 화면은 "아이 정보가
없어 결과를 만들지 못했습니다"라고 말했고, 아이는 멀쩡히 있었습니다.

지금은 백그라운드가 로그인을 스스로 관리하지 않습니다(`autoRefreshToken:
false` + `EmptyLocalStorage`). 화면이 쓰기 직전에 토큰을 실어 보냅니다.

### 로그인이 풀린 것을 듣는 곳을 하나로

위와 같은 뿌리입니다. 로그아웃되면 화면들이 "아이 정보를 먼저 등록해 주세요"
라고 말했습니다. 세션이 없으면 RLS가 오류가 아니라 **200 + 0행**을 주니까요.

`signedOut`을 듣는 곳을 만들었더니 이번에는 **두 곳이 됐습니다.** 설정 화면의
로그아웃과 `MainShell`의 리스너가 각각 로그인 화면을 세웠습니다. gotrue는
서버에 요청을 보내기 **전에** 로컬 세션을 지우고 `signedOut`을 흘리므로
리스너가 항상 먼저 돌고, HTTP 왕복을 기다리던 설정 화면이 뒤늦게 같은 이동을
한 번 더 해 방금 만든 화면을 지우고 다시 만들었습니다. 망이 나쁘면 그 위에
"로그아웃하지 못했습니다"까지 얹혔습니다 — 세션은 이미 지워진 뒤인데요.

`MainShell`에 달았던 것도 틀린 자리였습니다. `AuthGate`가 아이 없는 사용자를
온보딩으로 **직접** 보내므로 그 화면에서는 `MainShell`이 만들어지지 않습니다.
갓 가입한 사람이 아이 정보를 적는 동안 세션이 풀리면 아무도 보내지 않았고,
입력을 마치는 순간 저장이 실패했습니다. 화면마다 손으로 달면 하나씩 빠집니다.

지금은 `AuthRedirect`(`lib/core/services/auth_redirect.dart`) 하나가 앱 전체에서
듣습니다. `NavigatorObserver`를 물려받아 맨 위 라우트를 알고 있어서 이미 로그인
화면이면 다시 세우지 않고, 관찰자라 `navigator`도 딸려 오므로 이동에
`GlobalKey`가 따로 필요 없습니다.

인증 구독 두 곳에 `onError`도 달았습니다. gotrue 문서가 못 박아 둔 것입니다 —
이 흐름은 `BehaviorSubject`라 캐시한 에러를 새 구독자에게 그대로 다시 흘립니다.

### 그 밖

- **상담 입구를 홈 하나로.** 기록 화면 한가운데 전면 버튼 → 앱바 아이콘 →
  홈 오른쪽 아래 원형 단추로 두 번 옮겼습니다. 기록하는 중에 눈에 띄면
  방해가 되고, 방해가 안 되면 안 보였습니다. 입구가 하나이므로 그 대화는
  체온·수유·배변·수면·약병원 기록을 **전부** 맥락으로 받습니다.
- **`order()` 방향 누락 넷.** postgrest의 기본값이 내림차순인데 네 곳이
  오름차순인 줄 알고 썼습니다. 함께 키우기가 조용히 무효가 됐고, **출생 시
  몸무게가 '최근 성장 기록'으로** 상담에 나갔습니다.
- **수유 알림이 Android에서 아예 안 떴습니다.** 매니페스트에 리시버가 없어
  브로드캐스트가 예외도 로그도 없이 버려졌습니다.
- **기록 삭제에 확인 창.** 휴지통 한 번 탭이면 되돌릴 수 없었습니다.
- **완료한 접종은 눌러도 사라지지 않습니다.** 탭하면 날짜 수정, 되돌리기는
  길게 눌러 확인 창을 거칩니다.
- **탭이 굳던 것.** `IndexedStack`이라 `initState`가 한 번만 돌아, 방금 남긴
  기록이 '없음'으로 보였습니다. `RefreshSignal`을 탭마다 하나씩 두고
  보이는 탭에만 울립니다.
- **하루 평균을 7로 고정해 나누던 것.** 오늘 가입해 다섯 번 수유하면
  "하루 평균 0.7회"였습니다.

## 앞으로 해야 할 일

2026-09-01에 저장소를 여섯 갈래(기능·서버·사람 몫·미해결·검증·문서)로 나눠
훑고, 나온 주장 152건을 **반증하는 쪽으로** 한 번 더 확인했습니다. 136건이
남았습니다. 죽은 16건은 여기 적지 않았습니다 — 이를테면 "swiftpm 폴더가
빌드마다 다시 생긴다"는 지금 네 개가 있다는 것까지만 확인되고 재생성은
확인되지 않았습니다.

**문서만이 근거인 것은 뺐습니다.** 코드나 명령으로 다시 확인되지 않으면
적지 않았습니다.

### 당신이 해야 하는 일

Claude가 할 수 없는 것들입니다. 급한 순입니다.

| # | 할 일 | 어디서 | 왜 사람이어야 하나 |
|---|---|---|---|
| 1 | **서버를 올릴 곳 정하기** | 호스팅 업체 | 배포 파일이 하나도 없습니다 |
| 2 | **Anthropic 월 한도 걸기** | Anthropic Console | 상담·피부가 같은 지갑을 씁니다 |
| 3 | **`.pth` 9.3MB를 지울지 남길지** | 판단 | 아무 코드도 읽지 않습니다 |
| 4 | **마이크 −15dB 보정 확인** | 실물 소음계 | 하드웨어가 있어야 잽니다 |
| 5 | **실기기 확인** | 실제 휴대폰 | 이 환경에 기기가 없습니다 |
| 6 | **논문 12곳 고치기** | `.docx` 원고 | 원고가 저장소 밖입니다 |
| 7 | 애플 로그인 | Apple Developer | 연 $99 결제 |
| 8 | 스토어용 식별자·서명키 | 각 스토어 | 지금은 `com.example`에 디버그 키 |
| 9 | `swiftpm/` 넷을 무시할지 | 팀 합의 | `.gitignore`에 없습니다 |
| 10 | 브랜치 병합 시점 | 판단 | `origin/main`보다 75커밋 앞섭니다 |

**1번이 가장 큽니다.** `Dockerfile`·`docker-compose`·`Procfile`·`fly.toml`·
`render.yaml` 어느 것도 저장소에 없습니다. 지금 서버 주소 기본값은 개발
PC(안드로이드 `10.0.2.2`, 그 밖 `127.0.0.1`)라, **피부와 상담 두 기능은
개발 PC에서 서버를 띄워 둔 동안에만 동작합니다.** 심사 자리에서 노트북이
꺼져 있으면 그 둘은 "서버에 연결하지 못했습니다"로 끝납니다.

**5번은 시뮬레이터로 대신할 수 없습니다.** 알림의 절전 정책과 방해 금지,
마이크, 카메라, 밤새 소음 측정, 함께 키우기(계정 두 개)가 그렇습니다.
`integration_test/`도 없어 기기에서 자동으로 도는 시험은 0건입니다.

기기에서 볼 순서는 이렇습니다.

1. **밤새 소음 측정** — 저녁에 시작 → 앱 나감 → **아침에 들어와 중지.**
   여기서 판정이 안 나오던 적이 있습니다. 로그아웃되지 않는지도 함께 보세요
2. **알림** — macOS에서는 10초 시험으로 확인했습니다. 절전 정책과 방해 금지는
   데스크톱에 없어 실기기에서 다시 봐야 합니다
3. **함께 키우기** — 계정 두 개로 초대 → 수락 → **기록까지** 보이는지
4. **다크 모드** — 위 3·4번이 눈으로 드러나는 자리입니다

**소음 측정은 macOS에서 아예 안 됩니다.** `flutter_background_service`·
`noise_meter`·`permission_handler`가 macOS에 붙지 않습니다. 데스크톱으로
시연하실 거면 이 기능은 빼고 잡으셔야 합니다.

**6번 목록은 이미 만들어져 있습니다** —
[`docs/thesis-revision.md`](docs/thesis-revision.md)의 12건입니다. 가장 큰
것은 9번(피부를 자체 CNN·외부 API 미호출로 서술)입니다. 코드는 Claude
비전을 부르므로 그 서술과 다릅니다. 팀원은 CNN을 만들었다고 알고 계시니
합의가 먼저입니다. 원고는 `.gitignore`가 `*.docx`를 빼고 있어 저장소에
없습니다.

피부 데이터는 따로 조사해 뒀습니다(**저장소 밖 사실이라 명령으로는 다시
확인되지 않습니다**). AI Hub의 소아청소년 피부질환 이미지가 내용은 가장
맞지만 오프라인 안심존이라 서류가 여럿 필요하고 데이터를 밖으로 낼 수
없습니다. PASSION은 받을 수 있으나 **학습한 모델까지 비상업 전용**입니다.
SCIN은 기여자가 18세 이상뿐입니다. **신청 없이 받아 영유아 병명 분류에 쓸
데이터는 없었습니다.** 지금 저장소의 체크포인트는 클래스 이름이 성인 병변
(Actinic keratosis, Melanocytic nevus 등)이고 학습 코드도 노트북도 없어
**재현할 수 없습니다.**

**FCM은 사람 몫과 코드 몫이 겹칩니다.** 설정 파일 세 개가 다 없는 것은
맞지만, **파일을 받아 와도 붙지 않습니다** — `lib/`에 `firebase_messaging`
import가 0건이고, 안드로이드 Gradle에 `google-services` 선언이 없고, iOS에는
entitlements 파일 자체가 없습니다. `device_tokens` 표도 아무도 쓰지 않습니다.

**소셜 로그인은 이제 할 일이 아닙니다.** 예전 README가 "Supabase 대시보드에서
Google·Kakao 활성화"를 남은 일로 적었는데, 운영에서 **둘 다 이미 켜져
있습니다.** 남은 것은 애플뿐이고, 지금 애플 단추는 로그인을 하지 않고
"iOS 배포 시 지원 예정입니다" 스낵바만 띄웁니다.

**검증 계정 정리는 확인할 수단이 없습니다.** `service_role` 키가 이 저장소에
없어 `auth.users`를 코드로 볼 수도 지울 수도 없습니다. 계정 목록
(`verify-check-*`, `community-*`, `mig-check-*`, `share-check-*`)은 2026-08-13
문서 기록일 뿐, **지금도 남아 있는지는 대시보드에서 직접 보셔야 합니다.**

### 제가 이어서 할 일

코드로 고칠 수 있는 것들입니다. 심각한 순입니다.

1. **피부 분석 결과가 저장되지 않습니다.** 서버를 불러 결과를 받아 그리기까지
   하는데 `skin_analysis_page.dart`에 저장 호출이 한 줄도 없습니다. **화면을
   나가면 사라집니다.** `skin_analyses` 표는 스키마에도 010 마이그레이션에도
   있는데 `lib/` 어디에서도 참조하지 않습니다.
2. **실기기에서는 서버에 닿지 못합니다.** `API_BASE_URL`이 `env.json`에도 IDE
   실행 설정에도 없어 기본값인 개발 PC 주소로 갑니다.
3. **예방접종 화면이 자기 색을 박아 두고 있습니다.** `#14B8A6`을 상수로 두고
   열두 곳에 씁니다. 밝은 테마에서 **2.49:1** — 큰 글씨 기준 3:1도 못 넘깁니다.
   다크가 아니라 **라이트가 더 나쁩니다.**
4. **primary를 전경으로 쓰는 35곳이 한 번도 검증된 적 없습니다.** 다크에서
   어두운 카드 위 3.00:1. 골든 테스트가 'primary **위의** 흰 글씨'만 재고
   primary를 글씨색으로 쓰는 자리는 보지 않습니다.
5. **커뮤니티 화면 3개(949줄)에 테스트가 없습니다.** 모델·서비스·갈래에는
   있는데 화면에는 위젯 테스트도 문자열 검사도 없습니다. 인증 진입점인
   `auth_gate.dart`·`post_auth_route.dart`도 0건입니다.
6. **`silent_failure_test.dart`가 글자만 봅니다.** 실패 경로에서 플래그가
   실제로 켜지는지 보는 쪽으로 옮겨야 같은 종류를 계속 잡습니다.
7. **서머타임 계산 셋.** 한국만 쓰면 드러나지 않아 미뤘습니다. 다만 7일 창
   계산이 `analysis_page.dart:104`에도 복제돼 있어 **한쪽만 고치면 '조회 창 =
   집계 창'이 어긋납니다.** 고칠 때 함께 봐야 합니다.
8. **ERD 명세가 19개 표 중 13개만 다룹니다.** 빠진 여섯은 `posts`·`comments`·
   `baby_members`·`baby_invites`·`medication_records`·`hospital_visits`입니다.
   (예전 README는 12개라고 적었는데 틀렸습니다.)
9. **죽은 것들.** 쓰지 않는 의존성 셋(`desktop_drop`·`file_picker`·`http`),
   아무도 부르지 않는 백그라운드 백색소음 경로, `TodaySummary.hasAny`.
10. **`server/venv`에 `torch 2.13.0`이 있는데 `requirements.txt`에 없습니다.**
    requirements로 환경을 다시 만들면 지금 머신과 달라집니다.
11. **FastAPI 기본 문서 경로가 열려 있습니다.** `/docs`·`/redoc`·
    `/openapi.json`에 인증이 없습니다.
12. **'전체' 탭 네 항목이 '준비 중입니다'입니다.** 육아 가이드·진료용 리포트·
    공지사항·1:1 문의. 설정의 '기록 알림'도 같습니다.

### 문서가 코드와 어긋난 곳

**이 문서들은 지금 틀린 지시를 합니다.** 읽고 그대로 따르면 안 됩니다.

- **`server/README.md`** — 피부를 "모델 없음 → 503"이라 적고 `SKIN_MODEL_PATH`
  환경변수와 `~/baby_skin_data`(9종)를 안내합니다. **그 환경변수도 그 경로도
  없습니다.** 지금은 Claude 비전으로 동작하고 로그인까지 요구합니다.
  `MAX_UPLOAD_BYTES` 기본값도 50MB라 적었지만 실제는 4MB입니다.
- **`docs/review-findings.md`** — 39건 중 **23건이 이미 고쳐졌는데** 문서에는
  전부 미완으로 남아 있습니다. 머리말과 목록이 서로 어긋나기도 합니다.
- **`docs/supabase-todo.md`** — B절이 "모델이 없으니 피부 저장 보류"라 하고
  `disease_result`에 라벨을 넣으라 지시하는데, **같은 문서가 적용됐다고 적은
  010 마이그레이션이 그 칸을 지웠습니다.**
- **`docs/erd.md`** — 지워진 `disease_result`·`probability`가 명세에 남아
  있습니다.
- **`docs/remaining-work.md`** — 표를 20개라 하고(19개), 소셜 로그인이 꺼져
  있다 하고(켜져 있음), 예방접종 알림이 FCM이라 합니다(기기 안 로컬 알림).
- **`docs/erd-en.svg`** — 없는 `sleep_noise_logs` 상자가 남아 있고, 실제 표
  여섯은 그림에 없습니다.

## 검증 상태

**자동 테스트 — Flutter 711건 / 서버 109건**

`flutter test`, `cd server && ./venv/bin/python -m pytest`.
`flutter analyze`는 경고 0건입니다.

이 저장소의 테스트에는 **소스를 글자로 읽어 확인하는 것**이 섞여 있습니다.
화면 코드에 위젯 테스트를 붙이기 어려운 자리가 많은데, 거기서 되돌아가면
사용자가 데이터를 잃기 때문입니다. 예:

- `test/query_order_test.dart` — 방향 없는 `.order(`를 막습니다.
  한 곳을 고치려고 쓴 테스트가 **나머지 셋을 찾았습니다**
- `test/silent_failure_test.dart` — 조회 실패를 '기록 없음'으로 보여주지
  않는지
- `test/background_auth_test.dart` — 백그라운드가 로그인 저장소를 공유하지
  않는지

**테스트가 헛도는지 되돌려서 확인합니다.**

기록 탭을 고치며 붙인 회귀 테스트 하나가 **지키려는 코드를 통째로 지워도
통과**했습니다 — 자리를 견주는데 `indexOf`가 -1을 돌려주면 그 비교가 참이
됩니다. 그 뒤로는 고친 것을 하나씩 되돌려 놓고 테스트가 정말 깨지는지
확인했습니다. 이 방식이 실제로 구멍을 찾아냈습니다:

- 순수 함수는 잘 덮여 있는데 **거기에 값을 넘기는 배선**이 비어 있던 자리가
  세 곳 있었습니다. 상수를 넘기도록 바꿔도 단언이 전부 초록이었습니다.
  이번에 결함이 난 곳도 계산이 아니라 그 배선이었습니다
- 되돌리기 스무 가지 넘게 시험해 전부 잡히는 것을 확인했습니다

글자로 읽는 방식의 한계도 겪었습니다. 로그인 화면으로 **두 번** 보내던 결함을
그 테스트들은 하나도 잡지 못했습니다 — 찾던 문자열은 멀쩡히 다 있었으니까요.
그래서 인증 쪽은 실제로 돌리는 테스트로 옮겼습니다:

- `test/core/services/auth_redirect_test.dart` — 가짜 흐름을 물려 **화면 전환을
  직접 확인합니다.** 로그인 화면으로 정말 가는지, 두 번 가지 않는지, 흐름에
  실린 오류가 앱을 죽이지 않는지. 가드와 `onError`를 각각 빼고 돌려 이 테스트가
  깨지는 것까지 확인했습니다(그래야 테스트가 비어 있지 않다는 뜻이므로)

`AuthRedirect`가 Supabase에 직접 붙지 않고 흐름을 **받기만** 하도록 만든 것이
이걸 가능하게 했습니다. 앞으로 인증 주변을 고칠 때는 이 자리를 쓰면 됩니다.

**운영 DB에 직접 확인 (2026-08-13)**

001–011 마이그레이션이 전부 적용됐습니다. REST로 직접 물어 확인했습니다 —
`sleep_noise_logs`가 404(PGRST205), `sleep_noise_stats()`가 404(PGRST202),
`skin_analyses.disease_result`가 42703.

**로컬 PostgreSQL**

010·011을 두 번 적용해도 되는지, 마이그레이션 경로와 `schema.sql` 단독
경로가 같은 결과(표 19 · 정책 32 · 함수 8)를 내는지 확인했습니다.

**실제 API 호출**

피부 분석을 한 번 실제로 불러 경로 전체를 확인했습니다. 그 과정에서
구조화 출력이 `maxItems`를 받지 않는다는 것(400)을 발견해 고쳤습니다.

**확인됨 (실기기)**

- 소음 측정을 시작하면 `sleep_records`에 행이 생기고 `sleep_type`이 화면에서
  고른 값과 일치합니다. 백그라운드 isolate의 Supabase 초기화가 동작한다는
  뜻이기도 합니다.

**확인됨 (iOS 시뮬레이터, 2026-08-06)**

- iPhone 17 Pro에서 빌드·실행, Supabase 세션 복원, 마이크 권한 요청 정상.
- Firebase는 설정 파일이 없어 초기화가 실패하지만 **앱은 계속 동작합니다.**

**확인 못 한 것**

- Android APK 빌드 — 이 개발 머신에 Android SDK가 없습니다.
  매니페스트는 XML 유효성과 리시버 클래스 존재까지 대조했지만, Gradle 병합은
  `flutter run` 할 때 확인됩니다.

## 플랫폼 차이

- iOS는 `BGAppRefreshTask` 기반이라 **연속 소음 측정이 불가능**합니다. Android는
  foreground service로 계속 측정할 수 있습니다. 두 플랫폼의 기능을 동일하게 문서화하면 안 됩니다.
