<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- 기기를 다시 켜도 수유 알림 예약이 살아 있게 합니다.
         이게 없으면 재부팅 한 번에 잡아 둔 알림이 전부 사라집니다. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:label="BabySense"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:foregroundServiceType="microphone|mediaPlayback"
            android:exported="true">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE" android:value="BabySense Sleep Sound Monitor"/>
        </service>

        <!-- 수유 알림을 실제로 띄우는 리시버들.
             flutter_local_notifications는 예약을 PendingIntent.getBroadcast로
             걸어 두는데, 매니페스트에 선언되지 않은 컴포넌트로 가는 브로드캐스트는
             **예외도 로그도 없이 버려집니다.** 그래서 이 두 줄이 없으면 알림이
             영영 뜨지 않고, 화면에는 아무 단서도 남지 않습니다.
             플러그인 자체 매니페스트에는 권한만 있어 앱이 직접 넣어야 합니다
             (flutter_local_notifications README). -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />

        <!-- 재부팅·앱 업데이트 뒤 예약을 다시 겁니다. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- 소셜 로그인(OAuth) 콜백용 딥링크.
                 브라우저에서 로그인을 마치면 babysense://login-callback 으로 앱에 돌아옵니다.
                 Supabase 대시보드의 Redirect URLs에도 같은 주소를 등록해야 합니다. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="babysense" android:host="login-callback"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- 소셜 로그인은 url_launcher로 외부 브라우저를 엽니다.
             Android 11(API 30)부터는 이 선언이 없으면 브라우저를 찾지 못합니다. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <category android:name="android.intent.category.BROWSABLE"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>